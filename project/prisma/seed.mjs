async function main() {
  console.log("Autenticacao via Google OAuth — nenhum seed necessario.");
  console.log("Usuarios sao criados automaticamente no primeiro login.");
}

main().catch(console.error);
